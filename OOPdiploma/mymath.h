#pragma once

#include <cmath>
#include <exception>

typedef long long ll;
typedef long double ld;

const int MAXLEN = 1005000;
const int MAX_POW = 10000000;
const ll p = 31;

ll get_power(int t_power);

int get_log(int k);